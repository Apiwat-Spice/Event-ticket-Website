<!doctype html>
<html>

<head>
  <meta charset="utf-8">
  <title>Event Ticketing</title>
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="/css/style.css">
  <style>
    body {
      background-color: #121212;
      color: white;
      font-family: 'Prompt', sans-serif;
    }

    .wallet-card {
      background: linear-gradient(135deg, #ff7b00, #ff0080);
      color: white;
      border-radius: 20px;
      padding: 2rem;
      text-align: center;
      box-shadow: 0 4px 10px rgba(255, 255, 255, 0.2);
      max-width: 400px;
      margin: 100px auto;
    }

    .coins {
      font-size: 3rem;
      font-weight: bold;
    }

    .btn-add {
      margin-top: 1rem;
      background-color: white;
      color: #ff0080;
      border: none;
      border-radius: 10px;
      padding: 0.6rem 1.5rem;
      font-weight: bold;
      transition: 0.3s;
    }

    .btn-add:hover {
      background-color: #ff7b00;
      color: white;
    }
    .container{
      display: flex;
    }
  </style>
</head>
<nav class="navbar navbar-expand-lg navbar-light bg-light mb-4">
  <div class="container">
    <a class="navbar-brand" href="/events">EventTicket</a>
    <div class="collapse navbar-collapse">
      <ul class="navbar-nav ms-auto">
        <% if (!currentUser) { %>
        <li class="nav-item"><a class="nav-link" href="/login">เข้าสู่ระบบ</a></li>
        <li class="nav-item"><a class="nav-link" href="/register">สมัครสมาชิก</a></li>
        <% } else { %>
        <li class="nav-item"><a class="nav-link" href="/profile">สวัสดี, <%= currentUser.name %></a></li>
        <% if (currentUser.role === 'organizer') { %>
        <li class="nav-item"><a class="nav-link" href="/organizer/dashboard">Dashboard</a></li>
        <li class="nav-item"><a class="nav-link" href="/organizer/events/new">สร้างอีเวนต์</a></li>
        <% } %>
        <li class="nav-item"><a class="nav-link" href="/wallet">เติมเงิน</a></li>
        <li class="nav-item"><a class="nav-link" href="/logout">ออกจากระบบ</a></li>
        <% } %>
      </ul>
    </div>
  </div>
</nav>

<body>
  <div class="container">
    <div class="wallet-card">
      <h2>กระเป๋าเหรียญของ</h2>
      <h2><%= user.name %></h2>
      <p class="coins"><%= user.coins %> Coins</p>
      <form action="/addcoin" method="POST">
        <label for="coin">กรอกจำนวนเหรียญ (1 บาท = 1 Coin)</label>
        <input type="number" id="coin" name="coin" required class="form-control mt-2 mb-3">
        <button type="submit" class="btn-add">เติมเหรียญ</button>

      </form>
    </div>
    <div class="wallet-card">
      <h2>กระเป๋าเหรียญของ</h2>
      <h2><%= user.name %></h2>
      <p class="coins"><%= user.coins %> Coins</p>
      <form action="/delcoin" method="POST">
        <label for="coin">กรอกจำนวนเหรียญ (1 บาท = 1 Coin)</label>
        <input type="number" id="coin" name="coin" required class="form-control mt-2 mb-3">
        <button type="submit" class="btn-add">ถอนเงิน</button>

      </form>
    </div>
  </div>
</body>

</html>